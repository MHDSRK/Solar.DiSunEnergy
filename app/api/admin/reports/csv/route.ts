import{NextResponse}from'next/server'
import{ensureLeadTable,getSql}from'@/lib/db'
import{requireAdmin}from'@/lib/adminAuth'
export async function GET(){try{await requireAdmin();await ensureLeadTable();const rows=await getSql()`SELECT lead_id,created_at,name,phone,district,area,bill,monthly_kwh,connection_category,recommended_kw,setup_cost,subsidy,financing_amount,customer_contribution,lead_status,source FROM leads ORDER BY created_at DESC`;const cols=['lead_id','created_at','name','phone','district','area','bill','monthly_kwh','connection_category','recommended_kw','setup_cost','subsidy','financing_amount','customer_contribution','lead_status','source'];const esc=(v:any)=>`"${String(v??'').replaceAll('"','""')}"`;const csv=[cols.join(','),...(rows as any[]).map(r=>cols.map(c=>esc(r[c])).join(','))].join('\n');return new NextResponse(csv,{headers:{'content-type':'text/csv; charset=utf-8','content-disposition':'attachment; filename="disun-leads.csv"'}})}catch(e){const u=e instanceof Error&&e.message==='UNAUTHORIZED';return NextResponse.json({success:false,message:u?'Unauthorized':'Failed'},{status:u?401:500})}}