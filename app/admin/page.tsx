'use client'

import { useEffect, useMemo, useState } from 'react'
import { CalendarDays, Check, ChevronDown, Clock3, History, Pencil, Plus, Trash2, X } from 'lucide-react'

type Row = Record<string, any>
const STAGES = ['SITE_SURVEY','MATERIAL_ORDERED','INSTALLATION_STARTED','INSTALLATION_COMPLETE','NET_METER_APPLIED','COMMISSIONED']
const dt = (v:any) => v ? new Date(v).toLocaleString('en-IN',{dateStyle:'medium',timeStyle:'short'}) : '—'
const dateOnly = (v:any) => v ? new Date(v).toLocaleDateString('en-IN',{day:'2-digit',month:'short',year:'numeric'}) : '—'
const timeOnly = (v:any) => v ? new Date(v).toLocaleTimeString('en-IN',{hour:'2-digit',minute:'2-digit'}) : '—'
const stageLabel = (v:string) => String(v||'SITE_SURVEY').replaceAll('_',' ').toLowerCase().replace(/\b\w/g,x=>x.toUpperCase())

export default function Admin(){
 const[session,setSession]=useState<any>(null),[password,setPassword]=useState(''),[err,setErr]=useState(''),[notice,setNotice]=useState('')
 const[leads,setLeads]=useState<Row[]>([]),[audit,setAudit]=useState<Row[]>([]),[followups,setFollowups]=useState<Row[]>([]),[payments,setPayments]=useState<Row[]>([]),[stages,setStages]=useState<Row[]>([])
 const[tab,setTab]=useState<'SCHEDULE'|'LEADS'|'FOLLOWUPS'>('SCHEDULE'),[historyOpen,setHistoryOpen]=useState(false),[deleteMode,setDeleteMode]=useState(false),[selectedIds,setSelectedIds]=useState<string[]>([])
 const[expandedId,setExpandedId]=useState<string|null>(null),[newLeadOpen,setNewLeadOpen]=useState(false),[editLead,setEditLead]=useState<Row|null>(null),[paymentLead,setPaymentLead]=useState<string|null>(null),[paymentAmount,setPaymentAmount]=useState(""),[paymentMethod,setPaymentMethod]=useState(""),[paymentNote,setPaymentNote]=useState(""),[stageLead,setStageLead]=useState<string|null>(null),[customStageOpen,setCustomStageOpen]=useState(false),[customStage,setCustomStage]=useState('')
 const[form,setForm]=useState({name:'',phone:'',district:'',area:'',bill:'',monthly_kwh:'',connection_category:'Domestic',recommended_kw:''})

 const load=async()=>{const r=await fetch('/api/admin/leads',{cache:'no-store'});const d=await r.json();if(r.ok){setLeads(d.leads||[]);setAudit(d.audit||[]);setFollowups(d.followups||[]);setPayments(d.payments||[]);setStages(d.stages||[])}}
 useEffect(()=>{fetch('/api/admin/session').then(async r=>{const d=await r.json();setSession(d);if(d.authenticated)load()})},[])
 const login=async(e:any)=>{e.preventDefault();setErr('');try{const r=await fetch('/api/admin/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({password})});const d=await r.json().catch(()=>({}));if(!r.ok){setErr(d.message||'Login failed');return}setSession({authenticated:true,name:d.name,email:d.email});setPassword('');await load()}catch{setErr('Unable to connect to the server. Please try again.')}}
 const stageNames=useMemo(()=>[...new Set([...STAGES,...stages.map(x=>String(x.stage||'')).filter(Boolean)])],[stages])
 const currentStage=(id:string)=>stages.filter(x=>x.lead_id===id).sort((a,b)=>new Date(a.stage_at).getTime()-new Date(b.stage_at).getTime()).at(-1)?.stage||'SITE_SURVEY'
 const paid=(id:string)=>payments.filter(p=>p.lead_id===id).reduce((n,p)=>n+Number(p.amount||0),0)
 const todayFollowups=useMemo(()=>{const s=new Date();s.setHours(0,0,0,0);const e=new Date();e.setHours(23,59,59,999);return followups.filter(x=>x.status==='PENDING'&&new Date(x.follow_up_at)>=s&&new Date(x.follow_up_at)<=e)},[followups])
 const allSelected=leads.length>0&&selectedIds.length===leads.length
 const toggleAll=()=>setSelectedIds(allSelected?[]:leads.map(x=>String(x.lead_id)))
 const toggleSelect=(id:string)=>setSelectedIds(x=>x.includes(id)?x.filter(y=>y!==id):[...x,id])

 const deleteSelected=async()=>{if(!selectedIds.length)return;if(!confirm(`Delete ${selectedIds.length} selected lead(s)? This cannot be undone.`))return;const r=await fetch('/api/admin/leads',{method:'DELETE',headers:{'content-type':'application/json'},body:JSON.stringify({leadIds:selectedIds})});const d=await r.json();setNotice(d.message||(r.ok?'Deleted successfully.':'Delete failed.'));if(r.ok){setSelectedIds([]);setDeleteMode(false);setExpandedId(null);load()}}
 const deleteOne=async(id:string)=>{if(!confirm('Delete this lead? This cannot be undone.'))return;const r=await fetch('/api/admin/leads',{method:'DELETE',headers:{'content-type':'application/json'},body:JSON.stringify({leadIds:[id]})});const d=await r.json();setNotice(d.message||(r.ok?'Lead deleted.':'Delete failed.'));if(r.ok){setExpandedId(null);load()}}
 const create=async(e:any)=>{e.preventDefault();setNotice('');try{const payload={...form,phone:String(form.phone).replace(/\D/g,''),bill:String(form.bill).replace(/,/g,''),monthly_kwh:String(form.monthly_kwh).replace(/,/g,''),recommended_kw:String(form.recommended_kw).replace(/,/g,'')};const r=await fetch('/api/admin/leads',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});const d=await r.json().catch(()=>({}));if(!r.ok){setNotice(d.message||'Failed to create lead.');return}setNotice('Lead added successfully.');setForm({name:'',phone:'',district:'',area:'',bill:'',monthly_kwh:'',connection_category:'Domestic',recommended_kw:''});setNewLeadOpen(false);await load()}catch{setNotice('Unable to connect to the server. Please try again.')}}
 const updateStage=async(leadId:string,stage:string)=>{const r=await fetch('/api/admin/stages',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({leadId,stage})});const d=await r.json();if(!r.ok){setNotice(d.message||'Could not update stage.');return}setStageLead(null);setCustomStageOpen(false);setCustomStage('');await load()}
 const addCustomStage=()=>{const v=customStage.trim().toUpperCase().replace(/\s+/g,'_');if(v&&stageLead)updateStage(stageLead,v)}
 const addPayment=async(leadId:string)=>{const amount=Number(paymentAmount);if(!amount||amount<=0)return;const r=await fetch('/api/admin/payments',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({leadId,amount,method:paymentMethod,note:paymentNote})});const d=await r.json();setNotice(d.message||(r.ok?'Payment recorded.':'Payment failed.'));if(r.ok){setPaymentAmount('');setPaymentMethod('');setPaymentNote('');setPaymentLead(null);load()}}
 const saveEdit=async(e:any)=>{e.preventDefault();if(!editLead)return;const id=editLead.lead_id;const r=await fetch('/api/admin/leads/'+encodeURIComponent(id),{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify(editLead)});const d=await r.json();if(!r.ok){setNotice(d.message||'Could not update lead.');return}setNotice('Lead updated.');setEditLead(null);await load()}

 if(session===null)return <main className="min-h-dvh grid place-items-center bg-slate-950 text-white">Loading…</main>
 if(!session.authenticated)return <main className="min-h-dvh grid place-items-center bg-slate-950 p-5"><form onSubmit={login} className="w-full max-w-sm rounded-3xl bg-white p-6 text-slate-900 shadow-2xl"><p className="text-xs font-bold uppercase tracking-[0.2em] text-sky-600">DiSun Energy</p><h1 className="mt-1 text-2xl font-black">Admin Panel</h1><input required autoFocus type="password" placeholder="Enter admin password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-6 w-full rounded-2xl border border-slate-200 bg-white px-4 py-3 text-slate-900 outline-none focus:border-sky-500"/>{err&&<p className="mt-2 text-sm text-red-600">{err}</p>}<button className="mt-4 w-full rounded-2xl bg-sky-600 py-3 font-bold text-white">LOGIN</button></form></main>

 return <main className="min-h-dvh bg-slate-100 text-slate-900"><div className="mx-auto min-h-dvh max-w-5xl">
 <header className="sticky top-0 z-30 border-b border-slate-200 bg-white/95 px-4 py-3 backdrop-blur sm:px-6"><div className="flex items-center justify-between gap-3"><div><p className="text-[10px] font-bold uppercase tracking-[0.2em] text-sky-600">DiSun Energy</p><h1 className="text-xl font-black sm:text-2xl">Admin Panel</h1><p className="text-[11px] text-slate-500">Signed in as <b>{session.name}</b></p></div><div className="flex items-center gap-1"><button title="History" onClick={()=>setHistoryOpen(true)} className="grid size-10 place-items-center rounded-full hover:bg-slate-100"><History size={19}/></button><button title="Delete leads" onClick={()=>{setDeleteMode(x=>!x);setSelectedIds([])}} className={`grid size-10 place-items-center rounded-full ${deleteMode?'bg-red-50 text-red-600':'hover:bg-slate-100'}`}><Trash2 size={19}/></button><button onClick={async()=>{await fetch('/api/admin/logout',{method:'POST'});location.reload()}} className="ml-1 rounded-full bg-slate-900 px-3 py-2 text-xs font-bold text-white">Logout</button></div></div>
 <nav className="mt-3 grid grid-cols-3 gap-1 rounded-2xl bg-slate-100 p-1">{[['SCHEDULE',"Today's Schedule",todayFollowups.length],['LEADS','All Leads',leads.length],['FOLLOWUPS','Followups',followups.filter(x=>x.status==='PENDING').length]].map(([key,label,count])=><button key={String(key)} onClick={()=>setTab(key as any)} className={`rounded-xl px-2 py-2 text-[11px] font-bold sm:text-xs ${tab===key?'bg-white text-sky-700 shadow-sm':'text-slate-500'}`}>{label}<span className="ml-1 text-[10px] opacity-60">({count})</span></button>)}</nav></header>
 <section className="px-3 py-3 sm:px-6">{notice&&<div className="mb-3 flex items-center justify-between rounded-2xl border border-sky-100 bg-sky-50 px-4 py-3 text-sm text-sky-900"><span>{notice}</span><button onClick={()=>setNotice('')}><X size={16}/></button></div>}
 {tab==='LEADS'&&<><div className="mb-3 flex items-center justify-between"><div className="flex items-center gap-2">{deleteMode&&<button title="Select all" onClick={toggleAll} className="grid size-9 place-items-center rounded-xl border bg-white">{allSelected?<Check size={17}/>:<span className="text-sm">□</span>}</button>}<div><h2 className="text-lg font-black">All Leads</h2><p className="text-xs text-slate-500">{leads.length} total leads</p></div></div><button onClick={()=>setNewLeadOpen(true)} title="Add new lead" className="grid size-11 place-items-center rounded-full bg-sky-600 text-white shadow-lg shadow-sky-200"><Plus size={23}/></button></div>
 {deleteMode&&selectedIds.length>0&&<div className="sticky top-[126px] z-20 mb-3 flex items-center justify-between rounded-2xl bg-red-600 px-4 py-3 text-white shadow-lg"><span className="text-sm font-bold">{selectedIds.length} selected</span><button onClick={deleteSelected} className="rounded-full bg-white px-4 py-2 text-xs font-black text-red-600">DELETE</button></div>}
 <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="grid grid-cols-[1fr_1fr_auto] border-b bg-slate-50 px-3 py-2 text-[10px] font-bold uppercase tracking-wider text-slate-400 sm:px-4"><div className="flex items-center gap-2">{deleteMode&&<span className="w-8"/>}Name</div><div>Stage</div><div className="w-24 text-right">Payment</div></div>
 {leads.length===0&&<div className="p-8 text-center text-sm text-slate-400">No leads yet.</div>}
 {leads.map(l=>{const id=String(l.lead_id),expanded=expandedId===id,stage=currentStage(id);return <div key={id} className="border-b last:border-0"><div className="grid min-h-16 grid-cols-[1fr_1fr_auto] items-center gap-2 px-3 py-2 sm:px-4"><div className="flex min-w-0 items-center gap-2">{deleteMode&&<input type="checkbox" checked={selectedIds.includes(id)} onChange={()=>toggleSelect(id)} className="size-4 shrink-0 accent-red-600"/>}<button onClick={()=>setExpandedId(expanded?null:id)} className="min-w-0 text-left"><span className="block truncate text-sm font-bold">{l.name||'Unnamed lead'}</span><span className="block truncate text-[10px] text-slate-400">{id}</span></button></div><div className="relative"><button onClick={()=>setStageLead(stageLead===id?null:id)} className="flex max-w-full items-center gap-1 rounded-xl bg-slate-100 px-2.5 py-2 text-left text-[10px] font-bold text-slate-700"><span className="truncate">{stageLabel(stage)}</span><ChevronDown size={14} className="shrink-0"/></button>{stageLead===id&&<div className="absolute left-0 top-11 z-40 w-56 rounded-2xl border border-slate-200 bg-white p-2 shadow-xl">{stageNames.map(s=><button key={s} onClick={()=>updateStage(id,s)} className={`flex w-full items-center justify-between rounded-xl px-3 py-2 text-left text-xs hover:bg-slate-50 ${s===stage?'font-black text-sky-700':''}`}><span>{stageLabel(s)}</span>{s===stage&&<Check size={14}/>}</button>)}<button onClick={()=>setCustomStageOpen(true)} className="mt-1 flex w-full items-center gap-2 rounded-xl border-t px-3 py-3 text-xs font-bold text-sky-700"><Plus size={14}/> Add new stage</button>{customStageOpen&&<div className="mt-2 border-t pt-2"><input autoFocus value={customStage} onChange={e=>setCustomStage(e.target.value)} placeholder="New stage name" className="w-full rounded-xl border bg-white px-3 py-2 text-xs text-slate-900"/><button onClick={addCustomStage} className="mt-2 w-full rounded-xl bg-sky-600 py-2 text-xs font-bold text-white">ADD STAGE</button></div>}</div>}</div><div className="w-24 text-right text-xs font-bold">₹{paid(id).toLocaleString('en-IN')}</div></div>
 {expanded&&<div className="grid grid-cols-2 gap-2 bg-slate-50 px-3 pb-3 pt-2 sm:grid-cols-4 sm:px-4"><Info label="Phone" value={l.phone}/><Info label="Place" value={[l.area,l.district].filter(Boolean).join(', ')||'—'}/><Info label="Plant needed" value={l.recommended_kw?`${l.recommended_kw} kW`:'—'}/><Info label="Date & time" value={`${dateOnly(l.created_at)} · ${timeOnly(l.created_at)}`} /><Info label="Status" value={l.lead_status||'NEW'}/><Info label="Source" value={l.source||'web'}/><Info label="Customer contribution" value={l.customer_contribution?`₹${Number(l.customer_contribution).toLocaleString('en-IN')}`:'—'}/><div className="col-span-2 rounded-xl bg-white p-2.5"><p className="text-[9px] font-bold uppercase tracking-wide text-slate-400">Lead ID</p><div className="mt-1 flex items-center justify-between gap-2"><p className="break-all text-xs font-bold">{id}</p><div className="flex shrink-0 gap-1"><button title="Edit lead" onClick={()=>setEditLead({...l})} className="grid size-9 place-items-center rounded-xl bg-sky-50 text-sky-700"><Pencil size={16}/></button><button title="Delete lead" onClick={()=>deleteOne(id)} className="grid size-9 place-items-center rounded-xl bg-red-50 text-red-600"><Trash2 size={16}/></button></div></div></div>{paymentLead===id&&<div className="col-span-2 rounded-xl bg-white p-3"><p className="text-[9px] font-bold uppercase tracking-wide text-slate-400">Add payment</p><div className="mt-2 grid gap-2 sm:grid-cols-3"><input type="number" min="1" value={paymentAmount} onChange={e=>setPaymentAmount(e.target.value)} placeholder="Amount ₹" className="min-w-0 w-full rounded-xl border p-2.5 text-xs"/><input value={paymentMethod} onChange={e=>setPaymentMethod(e.target.value)} placeholder="Method" className="min-w-0 w-full rounded-xl border p-2.5 text-xs"/><input value={paymentNote} onChange={e=>setPaymentNote(e.target.value)} placeholder="Note" className="min-w-0 w-full rounded-xl border p-2.5 text-xs"/></div><button onClick={()=>addPayment(id)} className="mt-2 rounded-xl bg-sky-600 px-4 py-2 text-xs font-bold text-white">SAVE PAYMENT</button></div>}</div>}{paymentLead!==id&&<button onClick={()=>setPaymentLead(id)} className="col-span-2 rounded-xl border border-sky-100 bg-sky-50 py-2 text-xs font-bold text-sky-700">+ Add payment</button>}{stages.filter(x=>x.lead_id===id).length>0&&<div className="col-span-2 rounded-xl bg-white p-3"><p className="text-[9px] font-bold uppercase tracking-wide text-slate-400">Project timeline</p>{stages.filter(x=>x.lead_id===id).sort((a,b)=>new Date(a.stage_at).getTime()-new Date(b.stage_at).getTime()).map((x,i)=><div key={String(x.id||i)} className="mt-2 flex items-start gap-2 text-xs"><span className="mt-1 size-2 rounded-full bg-sky-600"/><div><b>{stageLabel(x.stage)}</b><p className="text-slate-400">{dt(x.stage_at)} · {x.recorded_by_name||'—'}</p></div></div>)}</div>}</div></div>}</div>})}</div></>}
 {tab==='SCHEDULE'&&<Schedule rows={todayFollowups} leads={leads} reload={load} openAdd={()=>setTab('FOLLOWUPS')}/>}
 {tab==='FOLLOWUPS'&&<Followups rows={followups} leads={leads} reload={load}/>}
 </section></div>
 {newLeadOpen&&<NewLeadModal form={form} setForm={setForm} close={()=>setNewLeadOpen(false)} create={create}/>}
 {editLead&&<EditLeadModal lead={editLead} setLead={setEditLead} close={()=>setEditLead(null)} save={saveEdit}/>}
 {historyOpen&&<HistoryModal rows={audit} close={()=>setHistoryOpen(false)}/>}
 </main>
}

function Info({label,value}:{label:string,value:any}){return <div className="rounded-xl bg-white p-2.5"><p className="text-[9px] font-bold uppercase tracking-wide text-slate-400">{label}</p><p className="mt-0.5 break-words text-xs font-bold">{String(value??'—')}</p></div>}

function NewLeadModal({form,setForm,close,create}:{form:Row,setForm:(v:any)=>void,close:()=>void,create:(e:any)=>void}){const numeric=['bill','monthly_kwh','recommended_kw'];return <Modal title="Add customer" eyebrow="New Lead" close={close}><form onSubmit={create} noValidate className="grid gap-3 sm:grid-cols-2">{Object.entries(form).map(([k,v])=>k==='connection_category'?<select key={k} required value={String(v)} onChange={e=>setForm({...form,[k]:e.target.value})} className="min-h-12 rounded-xl border border-slate-200 bg-white p-3 text-slate-900"><option>Domestic</option><option>Commercial</option></select>:<input key={k} required={['name','phone','district'].includes(k)} type={k==='phone'?'tel':'text'} inputMode={numeric.includes(k)||k==='phone'?'numeric':undefined} pattern={k==='phone'?'[0-9]{10}':undefined} placeholder={k.replaceAll('_',' ')} value={String(v)} onChange={e=>setForm({...form,[k]:e.target.value})} className="min-h-12 rounded-xl border border-slate-200 bg-white p-3 text-slate-900"/>)}<button type="submit" className="rounded-2xl bg-sky-600 p-3 font-bold text-white sm:col-span-2">ADD LEAD</button></form></Modal>}
function EditLeadModal({lead,setLead,close,save}:{lead:Row,setLead:(v:any)=>void,close:()=>void,save:(e:any)=>void}){const keys=['name','phone','district','area','recommended_kw','lead_status','connection_category','bill'];return <Modal title="Edit lead" eyebrow={String(lead.lead_id)} close={close}><form onSubmit={save} className="grid gap-3 sm:grid-cols-2">{keys.map(k=>k==='connection_category'?<select key={k} value={String(lead[k]??'Domestic')} onChange={e=>setLead({...lead,[k]:e.target.value})} className="min-h-12 rounded-xl border border-slate-200 bg-white p-3 text-slate-900"><option>Domestic</option><option>Commercial</option></select>:<input key={k} required={k==='name'||k==='phone'} type={k==='phone'||k==='bill'||k==='recommended_kw'?'number':'text'} placeholder={k.replaceAll('_',' ')} value={String(lead[k]??'')} onChange={e=>setLead({...lead,[k]:e.target.value})} className="min-h-12 rounded-xl border border-slate-200 bg-white p-3 text-slate-900"/>) }<button className="rounded-2xl bg-sky-600 p-3 font-bold text-white sm:col-span-2">SAVE CHANGES</button></form></Modal>}

function Modal({eyebrow,title,close,children}:{eyebrow:string,title:string,close:()=>void,children:any}){return <div className="fixed inset-0 z-50 overflow-auto bg-slate-950/60 p-3 sm:p-6"><div className="mx-auto mt-4 max-w-2xl rounded-3xl bg-white p-5 shadow-2xl sm:mt-10"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-sky-600">{eyebrow}</p><h2 className="text-xl font-black">{title}</h2></div><button onClick={close} className="grid size-9 place-items-center rounded-full bg-slate-100"><X size={18}/></button></div><div className="mt-5">{children}</div></div></div>}

function HistoryModal({rows,close}:{rows:Row[],close:()=>void}){return <Modal eyebrow="Audit history" title="All changes" close={close}><div className="max-h-[70dvh] overflow-auto">{rows.length?rows.map(x=><div key={x.id||JSON.stringify(x)} className="border-b py-4 last:border-0"><div className="flex items-start justify-between gap-3"><div><p className="text-sm font-bold">{x.field||'Change'}</p><p className="mt-1 text-xs text-slate-500">Lead: <b>{x.lead_id}</b></p></div><p className="shrink-0 text-[10px] text-slate-400">{dt(x.changed_at)}</p></div><p className="mt-2 text-xs"><span className="text-slate-400">{x.old_value||'—'}</span><span className="px-1">→</span><b>{x.new_value||'—'}</b></p><p className="mt-2 text-[10px] font-bold text-sky-700">Done by {x.changed_by_name||'—'} · {x.changed_by_email||'—'}</p></div>):<p className="py-8 text-center text-sm text-slate-400">No changes recorded.</p>}</div></Modal>}

function Schedule({rows,leads,reload,openAdd}:{rows:Row[],leads:Row[],reload:()=>void,openAdd:()=>void}){return <section><div className="mb-3 flex items-center gap-2"><CalendarDays size={20} className="text-sky-600"/><div><h2 className="text-lg font-black">Today's Schedule</h2><p className="text-xs text-slate-500">{rows.length} follow-up(s) due today</p></div></div>{rows.length>0&&<div className="mb-3 rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3 text-sm font-bold text-amber-900">{rows.length} follow-up{rows.length===1?'':'s'} scheduled for today.</div>}{rows.length?<FollowupList rows={rows} leads={leads} reload={reload}/>:<div className="rounded-3xl border bg-white p-6 text-center shadow-sm"><p className="text-2xl font-black leading-tight text-black sm:text-3xl">Nothing scheduled today?</p><p className="mx-auto mt-3 max-w-md text-base font-medium leading-7 text-slate-700 sm:text-lg">A grown man living like a parasite,<br/>waiting for a handout while<br/>doing absolutely nothing.!!!<br/>Don’t you feel ashamed.?</p><button onClick={openAdd} className="mt-6 inline-flex items-center gap-2 rounded-2xl bg-sky-600 px-5 py-3 text-sm font-bold text-white"><Plus size={17}/> Add</button></div>}</section>}

function Followups({rows,leads,reload}:{rows:Row[],leads:Row[],reload:()=>void}){return <section><div className="mb-3 flex items-center gap-2"><Clock3 size={20} className="text-sky-600"/><div><h2 className="text-lg font-black">Followups</h2><p className="text-xs text-slate-500">Manage scheduled customer follow-ups</p></div></div><FollowupList rows={rows} leads={leads} reload={reload}/></section>}

function FollowupList({rows,leads,reload}:{rows:Row[],leads:Row[],reload:()=>void}){const[leadId,setLeadId]=useState(''),[at,setAt]=useState(''),[note,setNote]=useState('');const add=async()=>{if(!leadId||!at)return;const r=await fetch('/api/admin/followups',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({leadId,followUpAt:at,note})});if(r.ok){setAt('');setNote('');setLeadId('');reload()}};const act=async(id:number,status:string)=>{await fetch('/api/admin/followups',{method:'PATCH',headers:{'content-type':'application/json'},body:JSON.stringify({id,status})});reload()};return <div className="grid gap-3 md:grid-cols-[300px_1fr]"><div className="rounded-3xl border border-slate-200 bg-white p-5 shadow-sm"><h3 className="font-black">Add follow-up</h3><label className="mt-4 block text-[11px] font-bold uppercase tracking-wide text-slate-400">Lead</label><select value={leadId} onChange={e=>setLeadId(e.target.value)} className="box-border mt-1 min-h-12 w-full max-w-full appearance-auto rounded-xl border border-slate-200 bg-white px-3 text-sm text-slate-900"><option value="">Select lead</option>{leads.map(l=><option key={l.lead_id} value={l.lead_id}>{l.name||'Unnamed lead'}</option>)}</select><label className="mt-3 block text-[11px] font-bold uppercase tracking-wide text-slate-400">Date & time</label><input type="datetime-local" value={at} onChange={e=>setAt(e.target.value)} className="box-border mt-1 block min-h-12 min-w-0 w-full max-w-full appearance-auto overflow-hidden rounded-xl border border-slate-200 bg-white px-3 text-sm text-slate-900 [color-scheme:light]"/><label className="mt-3 block text-[11px] font-bold uppercase tracking-wide text-slate-400">Note</label><textarea value={note} onChange={e=>setNote(e.target.value)} placeholder="Note" className="mt-1 min-h-24 w-full rounded-xl border border-slate-200 bg-white p-3 text-sm text-slate-900"/><button onClick={add} disabled={!leadId||!at} className="mt-3 w-full rounded-2xl bg-sky-600 py-3 text-sm font-bold text-white disabled:opacity-40">ADD FOLLOW-UP</button></div><div className="rounded-3xl border border-slate-200 bg-white shadow-sm">{rows.length?rows.map(x=><div key={x.id} className="border-b p-4 last:border-0"><div className="flex items-start justify-between gap-3"><div><b className="text-sm">{x.lead_name||leads.find(l=>String(l.lead_id)===String(x.lead_id))?.name||'Unnamed lead'}</b><p className="mt-1 text-xs text-slate-500">{dt(x.follow_up_at)}</p></div><span className="rounded-full bg-slate-100 px-2 py-1 text-[10px] font-bold">{x.status}</span></div><p className="mt-2 text-xs">{x.note||'—'}</p>{x.status==='PENDING'&&<div className="mt-3 flex gap-2"><button onClick={()=>act(x.id,'DONE')} className="rounded-full bg-emerald-600 px-3 py-2 text-xs font-bold text-white">DONE</button><button onClick={()=>act(x.id,'SNOOZED')} className="rounded-full border px-3 py-2 text-xs font-bold">SNOOZE</button></div>}</div>):<div className="p-8 text-center text-sm text-slate-400">No follow-ups.</div>}</div></div>}
