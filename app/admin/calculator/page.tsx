'use client'
import{useState}from'react'
export default function Calculator(){const[v,setV]=useState(''),[mode,setMode]=useState('bill'),[cat,setCat]=useState('Domestic'),[r,setR]=useState<any>(null);const run=async()=>{const x=await fetch('/api/admin/calculator',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({input:v,mode,category:cat})});setR(await x.json())};return <main className="min-h-dvh bg-[#03132f] p-4"><div className="mx-auto max-w-xl rounded-2xl bg-white p-5"><h1 className="text-2xl font-black text-[#1260a4]">Admin Solar Calculator</h1><select value={mode} onChange={e=>setMode(e.target.value)} className="mt-4 w-full rounded border p-3"><option value="bill">Monthly bill ₹</option><option value="units">Monthly units kWh</option></select><select value={cat} onChange={e=>setCat(e.target.value)} className="mt-2 w-full rounded border p-3"><option>Domestic</option><option>Commercial</option></select><input type="number" min="1" value={v} onChange={e=>setV(e.target.value)} className="mt-2 w-full rounded border p-3"/><button onClick={run} className="mt-3 w-full rounded-full bg-[#1260a4] p-3 font-bold text-white">CALCULATE</button>{r?.result&&<pre className="mt-4 overflow-auto rounded bg-slate-100 p-3 text-xs">{JSON.stringify(r.result,null,2)}</pre>}</div></main>}
