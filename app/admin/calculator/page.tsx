'use client'
import { useState } from 'react'
import { calculateSolarResult, type CalculationMode } from '@/services/solar/calculator'
export default function CalculatorPage() { const [value,setValue]=useState(''); const [category,setCategory]=useState('Domestic'); const [mode,setMode]=useState<CalculationMode>('bill'); const result=value ? calculateSolarResult(Number(value),category,mode) : null; return <main className="min-h-dvh bg-[#03132f] p-4 text-[#071528] sm:p-6"><div className="mx-auto max-w-lg rounded-[24px] bg-white p-5"><a href="/admin" className="text-xs font-bold text-[#1260a4]">← Admin</a><h1 className="mt-4 text-2xl font-extrabold text-[#1260a4]">Solar calculator</h1><div className="mt-5 grid gap-3"><label className="text-xs font-bold">MODE<select value={mode} onChange={e=>setMode(e.target.value as CalculationMode)} className="mt-1 h-11 w-full rounded-lg border p-2 text-base"><option value="bill">Monthly bill</option><option value="units">Monthly units</option></select></label><label className="text-xs font-bold">CATEGORY<select value={category} onChange={e=>setCategory(e.target.value)} className="mt-1 h-11 w-full rounded-lg border p-2 text-base"><option>Domestic</option><option>Commercial</option></select></label><label className="text-xs font-bold">VALUE<input required inputMode="decimal" value={value} onChange={e=>setValue(e.target.value)} className="mt-1 h-11 w-full rounded-lg border p-2 text-base" /></label></div>{result && <div className="mt-5 grid grid-cols-2 gap-3">{Object.entries(result).map(([key,item])=><div className="rounded-xl bg-slate-50 p-3" key={key}><div className="text-[9px] font-bold uppercase text-slate-400">{key}</div><div className="mt-1 font-extrabold text-[#1260a4]">{item}</div></div>)}</div>}</div></main> }
