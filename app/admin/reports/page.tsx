'use client'
import{useEffect,useState}from'react'
export default function Reports(){const[d,setD]=useState<any>(null);const[from,setFrom]=useState(''),[to,setTo]=useState('');const load=async()=>{const q=new URLSearchParams();if(from)q.set('from',from);if(to)q.set('to',to);const r=await fetch('/api/admin/reports?'+q.toString());setD(await r.json())};useEffect(()=>{load()},[]);return <main className="min-h-dvh bg-[#03132f] p-4"><div className="mx-auto max-w-6xl"><div className="rounded-2xl bg-white p-4"><h1 className="text-2xl font-black text-[#1260a4]">DiSun Reports</h1><div className="mt-3 flex flex-wrap gap-2"><input type="date" value={from} onChange={e=>setFrom(e.target.value)} className="rounded border p-2"/><input type="date" value={to} onChange={e=>setTo(e.target.value)} className="rounded border p-2"/><button onClick={load} className="rounded-full bg-[#1260a4] px-4 py-2 text-xs font-bold text-white">RUN REPORT</button></div></div>{d?.success&&<div className="mt-3 grid gap-3 md:grid-cols-3"><Card title="Funnel">{d.leads.map((x:any)=><p key={x.lead_status+x.source}>{x.lead_status} / {x.source}: <b>{x.count}</b></p>)}</Card><Card title="Payments">{d.payments.map((x:any)=><p key={x.recorded_by_name}>{x.recorded_by_name}: <b>₹{Number(x.total).toLocaleString('en-IN')}</b></p>)}</Card><Card title="Project stages">{d.stages.map((x:any)=><p key={x.stage}>{x.stage}: <b>{x.leads}</b></p>)}</Card></div>}</div></main>}
function Card({title,children}:{title:string,children:any}){return <section className="rounded-2xl bg-white p-4"><h2 className="mb-3 font-black">{title}</h2>{children}</section>}
